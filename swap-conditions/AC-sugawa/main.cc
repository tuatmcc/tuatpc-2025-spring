#include <bits/stdc++.h>

using namespace std;
typedef int_fast64_t i64;

int main() {
    i64 K, A[4], J[3], ans[4];
    cin >> K >> A[0] >> A[1] >> A[2] >> A[3] >> J[0] >> J[1] >> J[2];
    
    for(int r = 0; r < 4; r++) {
        
    }
    cout << ans[0] << " " << ans[1] << " " << ans[2] << " " << ans[3] << endl;
    return 0;   
}